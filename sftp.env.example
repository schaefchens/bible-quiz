# Credentials for scripts/deploy.sh. Copy to sftp.env and fill in.
# sftp.env is gitignored — never commit it.
#
# The account is jailed to the document root of
# https://biblequiz.games.schaefchens.de, so remote "/" is the web root.
SFTP_SERVER=user@www99.your-server.de
SFTP_PASSWD=your-sftp-password
