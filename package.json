{
  "name": "christ-biblionair",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "deploy": "bash scripts/deploy.sh",
    "deploy:dry": "bash scripts/deploy.sh --dry-run",
    "deploy:frontend": "bash scripts/deploy.sh --frontend",
    "deploy:backend": "bash scripts/deploy.sh --backend",
    "deploy:verify": "bash scripts/deploy.sh --verify-only"
  }
}
